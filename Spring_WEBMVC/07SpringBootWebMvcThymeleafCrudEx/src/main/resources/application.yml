#Server Config
server:
  port: 80
  servlet:
    context-path: /api/v1

#DatabaseConfig
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/empcrud
    username: root
    password: root

#JpaConfig

  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQLDialect
    show-sql: true
